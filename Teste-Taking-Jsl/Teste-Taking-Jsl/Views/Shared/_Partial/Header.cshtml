@*@using System.Security.Claims;
@using Gsn_ReclameAli.Common
@{
    var nomeCompleto = Context.User.GetNomeUsuario();
}*@
<header class="header navbar-area">
    <div class="container">
        <div class="row align-items-center">
            <div class="col-lg-12">
                <div class="nav-inner">
                    <!-- Start Navbar -->
                    <nav class="navbar navbar-expand-lg">

                        <button class="navbar-toggler mobile-menu-btn" type="button" data-bs-toggle="collapse"
                                data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent"
                                aria-expanded="false" aria-label="Toggle navigation">
                            <span class="toggler-icon"></span>
                            <span class="toggler-icon"></span>
                            <span class="toggler-icon"></span>
                        </button>
                        <div class="collapse navbar-collapse sub-menu-bar" id="navbarSupportedContent">
                            <ul id="nav" class="navbar-nav ms-auto">
                                <li class="nav-item">
                                    <a class="btn" href="@Url.Action("Index","Home")">Home</a>
                                </li>
                                <li class="nav-item">
                                    <a class="btn" href="@Url.Action("Index","Cliente")">Listar Clientes</a>
                                </li>
                                <li class="nav-item">
                                    <a class="btn" href="@Url.Action("ListarProblema","Problema")">Listar Produtos</a>
                                </li>
                                 <li class="nav-item">
                                    <a class="btn" href="@Url.Action("ListarProblema","Problema")">Listar Pedidos</a>
                                </li>
                            </ul>
                        </div> <!-- navbar collapse -->
                    </nav>
                    <!-- End Navbar -->
                </div>
            </div>
        </div> <!-- row -->
    </div>
</header>